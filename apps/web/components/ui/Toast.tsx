// This component is managed by ToastProvider
// Use the useToast() hook to show toasts:
//   const { showToast } = useToast();
//   showToast('Message', 'success' | 'error' | 'info');
export { useToast } from '@/providers/ToastProvider';
